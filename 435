if identifyexecutor() == 'Delta' then
    for _, v in pairs(game:GetService("CoreGui"):GetChildren()) do
        pcall(function()
            v:Destroy()
        end)
    end
    local gui = Instance.new("ScreenGui")
    gui.Name = "WarningGUI"
    gui.IgnoreGuiInset = true
    gui.ResetOnSpawn = false
    if syn and syn.protect_gui then
        syn.protect_gui(gui)
    end
    gui.Parent = game:GetService("CoreGui")

    -- Modern background with gradient
    local background = Instance.new("Frame")
    background.Size = UDim2.new(1, 0, 1, 0)
    background.Position = UDim2.new(0, 0, 0, 0)
    background.BackgroundColor3 = Color3.fromRGB(24, 24, 28)
    background.BorderSizePixel = 0
    background.Parent = gui
    local gradient = Instance.new("UIGradient")
    gradient.Color = ColorSequence.new{
        ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 30, 40)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(44, 44, 60))
    }
    gradient.Parent = background

    -- Add a semi-transparent dark overlay
    local overlay = Instance.new("Frame")
    overlay.Size = UDim2.new(1, 0, 1, 0)
    overlay.Position = UDim2.new(0, 0, 0, 0)
    overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    overlay.BackgroundTransparency = 0.35
    overlay.ZIndex = 1
    overlay.Parent = gui

    -- Modern container (larger, more padding, higher ZIndex)
    local container = Instance.new("Frame")
    container.AnchorPoint = Vector2.new(0.5, 0.5)
    container.Position = UDim2.new(0.5, 0, 0.5, 0)
    container.Size = UDim2.new(0.5, 0, 0.65, 0)
    container.BackgroundTransparency = 0
    container.BackgroundColor3 = Color3.fromRGB(34, 38, 54)
    container.BorderSizePixel = 0
    container.ZIndex = 2
    container.Parent = gui
    container.ClipsDescendants = false
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 22)
    corner.Parent = container
    local shadow = Instance.new("ImageLabel")
    shadow.BackgroundTransparency = 1
    shadow.Image = "rbxassetid://1316045217"
    shadow.Size = UDim2.new(1, 60, 1, 60)
    shadow.Position = UDim2.new(0, -30, 0, -30)
    shadow.ZIndex = 1
    shadow.ImageTransparency = 0.5
    shadow.Parent = container

    -- Data for executors
    local executors = {
        {name = "KRNL", link = "https://krnl.cat/"},
        {name = "Codex", link = "https://codex.lol/"},
        {name = "Arceus X", link = "https://spdmteam.com/index"},
        {name = "Fluxus", link = "https://fluxus.team/download/"},
    }

    -- Buttons container (vertical, more spacing)
    local buttonsContainer = Instance.new("Frame")
    buttonsContainer.Size = UDim2.new(1, -60, 0, #executors * 56)
    buttonsContainer.Position = UDim2.new(0, 30, 0, 240)
    buttonsContainer.BackgroundTransparency = 1
    buttonsContainer.ZIndex = 3
    buttonsContainer.Parent = container
    local list = Instance.new("UIListLayout")
    list.FillDirection = Enum.FillDirection.Vertical
    list.HorizontalAlignment = Enum.HorizontalAlignment.Center
    list.VerticalAlignment = Enum.VerticalAlignment.Top
    list.SortOrder = Enum.SortOrder.LayoutOrder
    list.Padding = UDim.new(0, 16)
    list.Parent = buttonsContainer

    -- Create modern buttons (with hover effect)
    for _, exec in ipairs(executors) do
        local btn = Instance.new("TextButton")
        btn.Text = "Copy " .. exec.name .. " Link"
        btn.TextScaled = true
        btn.Font = Enum.Font.GothamBold
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        btn.BorderSizePixel = 0
        btn.AutoButtonColor = false
        btn.Size = UDim2.new(0.92, 0, 0, 48)
        btn.ZIndex = 4
        btn.Parent = buttonsContainer
        local btnCorner = Instance.new("UICorner")
        btnCorner.CornerRadius = UDim.new(0, 12)
        btnCorner.Parent = btn
        local btnShadow = Instance.new("ImageLabel")
        btnShadow.BackgroundTransparency = 1
        btnShadow.Image = "rbxassetid://1316045217"
        btnShadow.Size = UDim2.new(1, 16, 1, 16)
        btnShadow.Position = UDim2.new(0, -8, 0, -8)
        btnShadow.ZIndex = 3
        btnShadow.ImageTransparency = 0.7
        btnShadow.Parent = btn
        btn.MouseEnter:Connect(function()
            btn.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
        end)
        btn.MouseLeave:Connect(function()
            btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        end)
        btn.MouseButton1Click:Connect(function()
            if setclipboard then
                setclipboard(exec.link)
            end
            btn.Text = "Copied!"
            btn.BackgroundColor3 = Color3.fromRGB(0, 220, 120)
            task.delay(1.5, function()
                btn.Text = "Copy " .. exec.name .. " Link"
                btn.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
            end)
        end)
    end

    -- Modern labels (high contrast, larger font, more spacing)
    local function createLabel(text, color, size, yOffset, bold)
        local label = Instance.new("TextLabel")
        label.BackgroundTransparency = 1
        label.Size = UDim2.new(1, -60, 0, size or 40)
        label.Position = UDim2.new(0, 30, 0, yOffset or 0)
        label.TextColor3 = color or Color3.fromRGB(255,255,255)
        label.Text = text
        label.Font = bold and Enum.Font.GothamBold or Enum.Font.Gotham
        label.TextScaled = true
        label.TextWrapped = true
        label.ZIndex = 5
        label.Parent = container
        return label
    end
    local y = 36
    createLabel("⚠️  [Delta Executor Detected]  ⚠️", Color3.fromRGB(255, 255, 0), 44, y, true)
    y = y + 44 + 10
    createLabel("Warning: Delta Executor has been detected as a virus on this application.", Color3.fromRGB(255, 0, 0), 36, y, true)
    y = y + 36 + 8
    createLabel("This software collects your personal information and is highly detected by antivirus systems.", Color3.fromRGB(255,255,255), 30, y)
    y = y + 30 + 8
    createLabel("Please consider using one of the following trusted executors:", Color3.fromRGB(200, 255, 255), 28, y)
    y = y + 28 + 6
    createLabel("(KRNL, Codex, Arceus X, Fluxus)", Color3.fromRGB(0, 255, 255), 26, y)
    y = y + 26 + 6
    createLabel("These executors are supported and verified as legitimate.", Color3.fromRGB(200, 255, 200), 26, y)

    -- Countdown label (bottom of container, high contrast)
    local countdownLabel = Instance.new("TextLabel")
    countdownLabel.AnchorPoint = Vector2.new(0.5, 1)
    countdownLabel.Position = UDim2.new(0.5, 0, 1, -24)
    countdownLabel.Size = UDim2.new(1, -60, 0, 38)
    countdownLabel.BackgroundTransparency = 1
    countdownLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    countdownLabel.TextStrokeTransparency = 0.7
    countdownLabel.TextScaled = true
    countdownLabel.Font = Enum.Font.GothamBold
    countdownLabel.Text = "Game will be closed in 30 seconds. Please install another executor."
    countdownLabel.ZIndex = 6
    countdownLabel.Parent = container

    -- Countdown logic
    local seconds = 30
    spawn(function()
        while seconds > 0 do
            countdownLabel.Text = "Game will be closed in " .. seconds .. " second" .. (seconds == 1 and "" or "s") .. ". Please install other executor"
            wait(1)
            seconds -= 1
        end
        local Players = game:GetService("Players")
        local player = Players.LocalPlayer
        game:Shutdown()
    end)
    wait(999999)
    return
end
loadstring(game:HttpGet("https://raw.githubusercontent.com/zanji-official/growagarden/refs/heads/main/3311"))()
